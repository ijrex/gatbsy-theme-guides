import { roboto as theme } from '@theme-ui/presets'

export default {
  ...theme,
  colors: {
    background: '#0cf',
    primary: '#000',
    modes: {
      dark: {
        background: '#000',
        primary: '#0cf',
      },
    },
  },
}
